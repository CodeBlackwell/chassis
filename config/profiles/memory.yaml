# Zero-dependency offline/CI profile: feature-hashing embedder + in-process
# brute-force store. No keys, no services, no heavy deps. Used by the smoke gate.
llm:         {impl: ollama, model: "llama3.1:8b"}
embedder:    {impl: hashing, dim: 256}
vectorstore: {impl: memory}
ingestion:   {chunk_size: 800, overlap: 120}
retrieval:   {k: 5}
memory:      {window: 8, recall_k: 3}
